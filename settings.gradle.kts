rootProject.name = "rekrutteringsbistand-stilling-indekser"

pluginManagement {
    repositories {
        gradlePluginPortal()
        maven (url="https://dl.bintray.com/gradle/gradle-plugins")
    }
}
